<!DOCTYPE html>
<html lang="en">

<head>
</head>
<main>
  <body>
    <div class="card-columns">
      <% @stories.each do |story| %>
          <div class="card">
            <% ActiveStorage::Current.set(host: "http://localhost:3000") do %>
              <a href="<%= url_for(story.media) %>" class="thumbnail">
                  <%= image_tag story.media.representation(resize: "300x300>").processed.service_url, class:"card-img" %>
              </a>
            <% end %>
          </div>
      <% end %>
    </div>
  </body>
</main>

</html>